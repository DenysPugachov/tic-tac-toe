Improvements   tic-tac-toe:


Archive:
- feat#5: When someone wins, highlight the three squares that caused the win. @done
- feat#4: Add a toggle button that lets you sort the moves in either ascending or descending order. @done
- feat#3: Rewrite Board to use two loops to make the squares instead of hardcoding them. @done
- Display the location for each move in the format (col, row) in the move history list. @done
- Bold the currently selected item in the move list. @done
- When no one wins, display a message about the result being a draw. @done 
